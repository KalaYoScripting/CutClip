name: Build and Release

on:
  push:
    branches: [main, master]
    tags: ['v*']
  pull_request:
    branches: [main, master]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore CutClip.sln

      - name: Build
        run: dotnet build CutClip.sln --configuration Release --no-restore

      - name: Publish (self-contained single file)
        run: >
          dotnet publish src/CutClip/CutClip.csproj
          --configuration Release
          --runtime win-x64
          --self-contained true
          -p:PublishSingleFile=true
          -p:IncludeNativeLibrariesForSelfExtract=true
          -o ./artifacts/build

      - name: Sign executable (optional)
        if: ${{ secrets.WINDOWS_SIGNING_CERT != '' }}
        shell: pwsh
        run: |
          $certBytes = [Convert]::FromBase64String("${{ secrets.WINDOWS_SIGNING_CERT }}")
          $certPath = "$env:RUNNER_TEMP\signing.pfx"
          [IO.File]::WriteAllBytes($certPath, $certBytes)
          & signtool sign /f $certPath /p "${{ secrets.WINDOWS_SIGNING_PASSWORD }}" /tr http://timestamp.digicert.com /td sha256 /fd sha256 ./artifacts/build/CutClip.exe

      - name: Install Inno Setup
        if: startsWith(github.ref, 'refs/tags/v')
        run: choco install innosetup --no-progress -y

      - name: Build installer
        if: startsWith(github.ref, 'refs/tags/v')
        shell: pwsh
        run: |
          $version = $env:GITHUB_REF_NAME -replace '^v',''
          $iscc = "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe"
          & $iscc installer/CutClip.iss "/DMyAppVersion=$version"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: CutClip-win-x64
          path: ./artifacts/build/CutClip.exe

      - name: Upload installer artifact
        if: startsWith(github.ref, 'refs/tags/v')
        uses: actions/upload-artifact@v4
        with:
          name: CutClip-installer
          path: ./artifacts/installer/CutClip-Setup-*.exe

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            ./artifacts/build/CutClip.exe
            ./artifacts/installer/CutClip-Setup-*.exe
          generate_release_notes: true
